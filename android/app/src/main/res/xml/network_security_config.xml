<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- If you need to allow specific domains for cleartext during development,
         add domain-config entries here and set cleartextTrafficPermitted="true" for them only. -->
</network-security-config>
